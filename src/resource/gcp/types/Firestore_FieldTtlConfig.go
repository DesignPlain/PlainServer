package types

type Firestore_FieldTtlConfig struct {
	/*
	   (Output)
	   The state of TTL (time-to-live) configuration for documents that have this Field set.
	*/
	State string `json:"state,omitempty" yaml:"state,omitempty"`
}
