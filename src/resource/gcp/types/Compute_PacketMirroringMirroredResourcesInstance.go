package types

type Compute_PacketMirroringMirroredResourcesInstance struct {
	/*
	   The URL of the instances where this rule should be active.

	   - - -
	*/
	Url string `json:"url,omitempty" yaml:"url,omitempty"`
}
