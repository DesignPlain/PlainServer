package types

type Sagemaker_DataQualityJobDefinitionJobResources struct {
	// The configuration for the cluster resources used to run the processing job. Fields are documented below.
	ClusterConfig Sagemaker_DataQualityJobDefinitionJobResourcesClusterConfig `json:"clusterConfig,omitempty" yaml:"clusterConfig,omitempty"`
}
