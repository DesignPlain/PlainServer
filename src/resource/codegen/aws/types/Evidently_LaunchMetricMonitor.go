package types

type Evidently_LaunchMetricMonitor struct {
	// A block that defines the metric. Detailed below.
	MetricDefinition Evidently_LaunchMetricMonitorMetricDefinition `json:"metricDefinition,omitempty" yaml:"metricDefinition,omitempty"`
}
