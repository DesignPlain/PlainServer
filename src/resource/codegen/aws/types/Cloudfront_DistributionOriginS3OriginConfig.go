package types

type Cloudfront_DistributionOriginS3OriginConfig struct {
	// The CloudFront origin access identity to associate with the origin.
	OriginAccessIdentity string `json:"originAccessIdentity,omitempty" yaml:"originAccessIdentity,omitempty"`
}
