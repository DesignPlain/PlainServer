package types

type Vpclattice_TargetGroupConfigHealthCheckMatcher struct {
	// The HTTP codes to use when checking for a successful response from a target.
	Value string `json:"value,omitempty" yaml:"value,omitempty"`
}
