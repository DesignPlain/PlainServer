package types

type Appmesh_GatewayRouteSpecHttp2RouteMatchQueryParameterMatch struct {
	// Header value sent by the client must match the specified value exactly.
	Exact string `json:"exact,omitempty" yaml:"exact,omitempty"`
}
